<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_estate_student_form" model="ir.ui.view">
        <field name="name">estate.student.form</field>
        <field name="model">estate.student</field>
        <field name="arch" type="xml">
            <form string="Student Form">
                <div class="profile">
                    <field name="profile" widget="image" style="height: 128px; width: 128px;"/>
                </div>

                <notebook>
                    <page string="User Information">
                        <button name="logger" type="object" string="log something"/>
                        <field name="name"/>
                        <field name="score"/>
                        <field name="dob"/>
                        <field name="gender"/>
                        <field name="age"/>
                    </page>
                </notebook>
            </form>
        </field>
    </record>
</odoo>
